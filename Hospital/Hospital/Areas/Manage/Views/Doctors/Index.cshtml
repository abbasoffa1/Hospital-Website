@model IEnumerable<Hospital.Models.Doctor>

@{
                /**/

                /**/

                ViewBag.Title = "Index";
                Layout = "~/Areas/Manage/Views/Shared/_LayoutManage.cshtml";
}
}
<section class="content">
    <div class="container-fluid">
        <div class="block-header">
            <h2>All Doctors</h2>
        </div>
        <div class="row clearfix">
            @foreach (var item in Model)
            {

                <div class="col-xl-3 col-lg-4 col-md-6 col-sm-12">
                    <div class="card">
                        <div class="body">
                            <div class="member-card verified">
                                <div class="thumb-xl member-thumb">
                                    <img src="~/Uploads/@item.Photo" class="img-thumbnail rounded-circle" alt="profile-image">
                                </div>

                                <div class="">
                                    <h4 class="m-b-5 m-t-20">@item.FullName</h4>
                                    <p class="text-muted">@item.Department.Name<span> <a href="javascript:void(0);" class="text-pink">@item.Email</a> </span></p>
                                </div>

                                <p class="text-muted">@item.Phone</p>
                                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
        <div class="row clearfix">
            <div class="col-sm-12 text-center">
                <a href="@Url.Action("create","Doctors")" class="btn btn-raised g-bg-cyan">Add Doctor</a>
            </div>
        </div>
    </div>
</section>


